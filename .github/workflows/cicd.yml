name: CICD

on:
  workflow_dispatch: 

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        run: echo "Code clone"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run build
        run: echo "Build Created"
  
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Running
        run: echo "Tested Successfully"
  
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment the test build
        run: echo "Deployed the build"
